---

- name: 'task include install'
  include: install.yml
  when: not (openbsd_netcat_state|match('false|no|remove|quiesce|inactive'))
  tags:
    - main
    - openbsd_netcat

- name: 'task include remove'
  include: remove.yml
  when: (openbsd_netcat_state|match('false|no|remove'))
  tags:
    - main
    - openbsd_netcat
